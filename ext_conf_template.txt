# cat=basic//010; type=int+; label= PID of Pattern Page: the PID of the page which contains the Design Pattern.
designPatternPid = 0

# cat=basic//020; type=text; label= FE-Domain: Add a domain for the frontend rendering (optional if FE domain is different to BE domain - e.g. https://www.domain.org)
domain =
